image: "node:latest"

stages:
  - build
  - test

build_app:
  stage: build
  script:
    - npx eslint --fix
    - npx lint-staged
    - npm install
    - npm run build
  artifacts:
    paths:
      - build
    expire_in: 1 day

test_app:
  stage: test
  script:
    - npm test -- --watchAll=false
